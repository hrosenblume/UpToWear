<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.upto.android"
    android:versionCode="35"
    android:versionName="3.1.1.1" >

    <uses-sdk
        android:minSdkVersion="20"
        android:targetSdkVersion="20" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher_3"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />
        <activity
            android:name="com.upto.android.MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ScheduleEventActivity"
            android:label="@string/title_activity_schedule_event" >
        </activity>
        <service
            android:name=".ScheduleEventService"
            android:exported="false" >
        </service>
        <service
            android:name=".StartDiscoverService"
            android:exported="false" >
        </service>
    </application>
</manifest>
